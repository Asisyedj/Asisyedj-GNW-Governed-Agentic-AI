# GNW V4 — 100 Independent Subjects: Deep Research + Remediation

**Rule:** This document defines the exact fix and evidence required for each subject. It never converts design into PASS.

## Research basis

OWASP AI Agent Security; NIST NCCoE 2026 agent identity/authorization; SPIFFE workload identity; Google Agent Gateway; SLSA 1.2 provenance; Anthropic filesystem/network sandboxing. The common conclusion is independent authorization, least privilege, short-lived identity, isolated execution, default-deny egress, adversarial testing and verifiable release evidence.

| ID | Subject | Fix | Exact test | Evidence | State |
|---|---|---|---|---|---|
| IRS-001 | Authority source | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass authority source and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-002 | Default deny | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass default deny and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-003 | Complete mediation | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass complete mediation and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-004 | Policy version binding | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass policy version binding and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-005 | Classification integrity | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass classification integrity and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-006 | Purpose integrity | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass purpose integrity and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-007 | Operation integrity | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass operation integrity and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-008 | Resource integrity | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass resource integrity and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-009 | Scope integrity | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass scope integrity and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-010 | Decision provenance | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass decision provenance and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-011 | Trusted subject identity | Trusted identity source; reject caller-supplied identity; short-lived workload identity. | Adversarially mutate/bypass trusted subject identity and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-012 | Identity expiry | Trusted identity source; reject caller-supplied identity; short-lived workload identity. | Adversarially mutate/bypass identity expiry and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-013 | Session revocation | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass session revocation and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-014 | Session fixation | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass session fixation and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-015 | Tenant binding | Trusted tenant binding plus object/query-level tenant predicates on every path. | Adversarially mutate/bypass tenant binding and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-016 | Tenant query isolation | Trusted tenant binding plus object/query-level tenant predicates on every path. | Adversarially mutate/bypass tenant query isolation and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-017 | Tenant context replay | Trusted tenant binding plus object/query-level tenant predicates on every path. | Adversarially mutate/bypass tenant context replay and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-018 | Role integrity | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass role integrity and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-019 | Membership freshness | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass membership freshness and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-020 | Foreign-ID existence leakage | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass foreign-id existence leakage and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-021 | Independent approver identity | Trusted identity source; reject caller-supplied identity; short-lived workload identity. | Adversarially mutate/bypass independent approver identity and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-022 | Separation of duties | Independent approver, exact action digest, request/grant/tenant/expiry binding and step-up for critical actions. | Adversarially mutate/bypass separation of duties and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-023 | Step-up authentication | Independent approver, exact action digest, request/grant/tenant/expiry binding and step-up for critical actions. | Adversarially mutate/bypass step-up authentication and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-024 | Exact approval digest | Independent approver, exact action digest, request/grant/tenant/expiry binding and step-up for critical actions. | Adversarially mutate/bypass exact approval digest and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-025 | Approval request binding | Independent approver, exact action digest, request/grant/tenant/expiry binding and step-up for critical actions. | Adversarially mutate/bypass approval request binding and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-026 | Approval grant binding | Independent approver, exact action digest, request/grant/tenant/expiry binding and step-up for critical actions. | Adversarially mutate/bypass approval grant binding and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-027 | Approval tenant binding | Trusted tenant binding plus object/query-level tenant predicates on every path. | Adversarially mutate/bypass approval tenant binding and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-028 | Approval expiry | Independent approver, exact action digest, request/grant/tenant/expiry binding and step-up for critical actions. | Adversarially mutate/bypass approval expiry and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-029 | Approval status integrity | Independent approver, exact action digest, request/grant/tenant/expiry binding and step-up for critical actions. | Adversarially mutate/bypass approval status integrity and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-030 | Approval preview fidelity | Independent approver, exact action digest, request/grant/tenant/expiry binding and step-up for critical actions. | Adversarially mutate/bypass approval preview fidelity and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-031 | Grant signature | Signed short-lived exact-bound artifact, durable atomic consumption and final interlock. | Adversarially mutate/bypass grant signature and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-032 | Grant issuer trust | Signed short-lived exact-bound artifact, durable atomic consumption and final interlock. | Adversarially mutate/bypass grant issuer trust and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-033 | Key ID trust | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass key id trust and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-034 | Grant nonce uniqueness | Signed short-lived exact-bound artifact, durable atomic consumption and final interlock. | Adversarially mutate/bypass grant nonce uniqueness and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-035 | Grant time validity | Signed short-lived exact-bound artifact, durable atomic consumption and final interlock. | Adversarially mutate/bypass grant time validity and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-036 | Grant TTL bound | Signed short-lived exact-bound artifact, durable atomic consumption and final interlock. | Adversarially mutate/bypass grant ttl bound and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-037 | Capability lease signature | Signed short-lived exact-bound artifact, durable atomic consumption and final interlock. | Adversarially mutate/bypass capability lease signature and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-038 | Capability lease binding | Signed short-lived exact-bound artifact, durable atomic consumption and final interlock. | Adversarially mutate/bypass capability lease binding and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-039 | Capability lease single use | Signed short-lived exact-bound artifact, durable atomic consumption and final interlock. | Adversarially mutate/bypass capability lease single use and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-040 | Final interlock | Signed short-lived exact-bound artifact, durable atomic consumption and final interlock. | Adversarially mutate/bypass final interlock and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-041 | Model authority isolation | Treat as untrusted data; strict schema/provenance/isolation; never authority. | Adversarially mutate/bypass model authority isolation and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — implementation + execution required |
| IRS-042 | Structured output validation | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass structured output validation and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — implementation + execution required |
| IRS-043 | Prompt injection isolation | Treat as untrusted data; strict schema/provenance/isolation; never authority. | Adversarially mutate/bypass prompt injection isolation and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — implementation + execution required |
| IRS-044 | Retrieved-content isolation | Treat as untrusted data; strict schema/provenance/isolation; never authority. | Adversarially mutate/bypass retrieved-content isolation and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — implementation + execution required |
| IRS-045 | Web-content isolation | Treat as untrusted data; strict schema/provenance/isolation; never authority. | Adversarially mutate/bypass web-content isolation and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — implementation + execution required |
| IRS-046 | Tool-output isolation | Signed/pinned registry, strict schema, least-privilege scope and gateway enforcement. | Adversarially mutate/bypass tool-output isolation and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — implementation + execution required |
| IRS-047 | Memory integrity | Treat as untrusted data; strict schema/provenance/isolation; never authority. | Adversarially mutate/bypass memory integrity and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — implementation + execution required |
| IRS-048 | Memory tenant isolation | Trusted tenant binding plus object/query-level tenant predicates on every path. | Adversarially mutate/bypass memory tenant isolation and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — implementation + execution required |
| IRS-049 | Memory lifecycle | Treat as untrusted data; strict schema/provenance/isolation; never authority. | Adversarially mutate/bypass memory lifecycle and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — implementation + execution required |
| IRS-050 | Sensitive-memory redaction | Treat as untrusted data; strict schema/provenance/isolation; never authority. | Adversarially mutate/bypass sensitive-memory redaction and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — implementation + execution required |
| IRS-051 | Agent identity | Trusted identity source; reject caller-supplied identity; short-lived workload identity. | Adversarially mutate/bypass agent identity and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — implementation + execution required |
| IRS-052 | Agent message authenticity | Distinct authenticated agent identity, signed messages, attenuated delegation and re-authorization. | Adversarially mutate/bypass agent message authenticity and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — implementation + execution required |
| IRS-053 | Agent message authorization | Distinct authenticated agent identity, signed messages, attenuated delegation and re-authorization. | Adversarially mutate/bypass agent message authorization and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — implementation + execution required |
| IRS-054 | Agent tenant binding | Trusted tenant binding plus object/query-level tenant predicates on every path. | Adversarially mutate/bypass agent tenant binding and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — implementation + execution required |
| IRS-055 | Agent capability attenuation | Signed short-lived exact-bound artifact, durable atomic consumption and final interlock. | Adversarially mutate/bypass agent capability attenuation and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — implementation + execution required |
| IRS-056 | Agent recursion limit | Distinct authenticated agent identity, signed messages, attenuated delegation and re-authorization. | Adversarially mutate/bypass agent recursion limit and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — implementation + execution required |
| IRS-057 | Tool registry integrity | Signed/pinned registry, strict schema, least-privilege scope and gateway enforcement. | Adversarially mutate/bypass tool registry integrity and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — implementation + execution required |
| IRS-058 | Tool schema integrity | Signed/pinned registry, strict schema, least-privilege scope and gateway enforcement. | Adversarially mutate/bypass tool schema integrity and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — implementation + execution required |
| IRS-059 | Tool scope separation | Independent approver, exact action digest, request/grant/tenant/expiry binding and step-up for critical actions. | Adversarially mutate/bypass tool scope separation and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — implementation + execution required |
| IRS-060 | Tool provenance | Signed/pinned registry, strict schema, least-privilege scope and gateway enforcement. | Adversarially mutate/bypass tool provenance and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — implementation + execution required |
| IRS-061 | Egress allowlist | Default-deny egress proxy, TLS validation, SSRF/private-IP/DNS-rebinding/redirect controls. | Adversarially mutate/bypass egress allowlist and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-062 | HTTPS enforcement | Default-deny egress proxy, TLS validation, SSRF/private-IP/DNS-rebinding/redirect controls. | Adversarially mutate/bypass https enforcement and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-063 | Private-address blocking | Default-deny egress proxy, TLS validation, SSRF/private-IP/DNS-rebinding/redirect controls. | Adversarially mutate/bypass private-address blocking and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-064 | Metadata endpoint blocking | Default-deny egress proxy, TLS validation, SSRF/private-IP/DNS-rebinding/redirect controls. | Adversarially mutate/bypass metadata endpoint blocking and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-065 | DNS rebinding resistance | Default-deny egress proxy, TLS validation, SSRF/private-IP/DNS-rebinding/redirect controls. | Adversarially mutate/bypass dns rebinding resistance and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — real environment/evidence required |
| IRS-066 | Redirect control | Default-deny egress proxy, TLS validation, SSRF/private-IP/DNS-rebinding/redirect controls. | Adversarially mutate/bypass redirect control and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-067 | Webhook destination binding | Default-deny egress proxy, TLS validation, SSRF/private-IP/DNS-rebinding/redirect controls. | Adversarially mutate/bypass webhook destination binding and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-068 | Credential isolation | Gateway/KMS secret isolation, redaction and classification/purpose-based minimization. | Adversarially mutate/bypass credential isolation and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-069 | Credential redaction | Gateway/KMS secret isolation, redaction and classification/purpose-based minimization. | Adversarially mutate/bypass credential redaction and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-070 | Data minimization | Gateway/KMS secret isolation, redaction and classification/purpose-based minimization. | Adversarially mutate/bypass data minimization and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-071 | Path traversal | Canonical namespace containment, no link escape and bounded streaming/size. | Adversarially mutate/bypass path traversal and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-072 | UNC/device paths | Canonical namespace containment, no link escape and bounded streaming/size. | Adversarially mutate/bypass unc/device paths and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-073 | Symlink/junction escape | Canonical namespace containment, no link escape and bounded streaming/size. | Adversarially mutate/bypass symlink/junction escape and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-074 | File size limit | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass file size limit and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-075 | Object storage tenant prefix | Trusted tenant binding plus object/query-level tenant predicates on every path. | Adversarially mutate/bypass object storage tenant prefix and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-076 | Database transaction integrity | Durable transactions, tenant-aware constraints and atomic reservation/consumption. | Adversarially mutate/bypass database transaction integrity and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-077 | Database constraints | Durable transactions, tenant-aware constraints and atomic reservation/consumption. | Adversarially mutate/bypass database constraints and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-078 | Audit pre-admission | Pre-action admission, exact persisted-field hashing, tamper detection and end-to-end correlation. | Adversarially mutate/bypass audit pre-admission and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-079 | Audit tamper detection | Pre-action admission, exact persisted-field hashing, tamper detection and end-to-end correlation. | Adversarially mutate/bypass audit tamper detection and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-080 | Audit completeness | Pre-action admission, exact persisted-field hashing, tamper detection and end-to-end correlation. | Adversarially mutate/bypass audit completeness and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-081 | Kill switch propagation | Fail-closed distributed operational controls, durable state, tested recovery and containment. | Adversarially mutate/bypass kill switch propagation and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-082 | Circuit breaker behavior | Fail-closed distributed operational controls, durable state, tested recovery and containment. | Adversarially mutate/bypass circuit breaker behavior and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-083 | In-flight cancellation | Fail-closed distributed operational controls, durable state, tested recovery and containment. | Adversarially mutate/bypass in-flight cancellation and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — real environment/evidence required |
| IRS-084 | Restart safety | Fail-closed distributed operational controls, durable state, tested recovery and containment. | Adversarially mutate/bypass restart safety and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-085 | Failover safety | Fail-closed distributed operational controls, durable state, tested recovery and containment. | Adversarially mutate/bypass failover safety and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-086 | Backup/restore safety | Fail-closed distributed operational controls, durable state, tested recovery and containment. | Adversarially mutate/bypass backup/restore safety and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — real environment/evidence required |
| IRS-087 | Secret rotation | Gateway/KMS secret isolation, redaction and classification/purpose-based minimization. | Adversarially mutate/bypass secret rotation and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — real environment/evidence required |
| IRS-088 | Key rotation | Fail-closed distributed operational controls, durable state, tested recovery and containment. | Adversarially mutate/bypass key rotation and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — real environment/evidence required |
| IRS-089 | Monitoring/alerting | Fail-closed distributed operational controls, durable state, tested recovery and containment. | Adversarially mutate/bypass monitoring/alerting and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-090 | Incident response | Fail-closed distributed operational controls, durable state, tested recovery and containment. | Adversarially mutate/bypass incident response and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | IMPLEMENTED/PARTIAL — execution proof required |
| IRS-091 | Dependency declaration | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass dependency declaration and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | BLOCKED — S3 runtime dependency/lockfile |
| IRS-092 | Dependency lock integrity | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass dependency lock integrity and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | BLOCKED — S3 runtime dependency/lockfile |
| IRS-093 | Dependency vulnerability gate | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass dependency vulnerability gate and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — real environment/evidence required |
| IRS-094 | SBOM completeness | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass sbom completeness and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — real environment/evidence required |
| IRS-095 | Build provenance | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass build provenance and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — real environment/evidence required |
| IRS-096 | Artifact signature | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass artifact signature and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — real environment/evidence required |
| IRS-097 | Container hardening | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass container hardening and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — real environment/evidence required |
| IRS-098 | Reproducibility | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass reproducibility and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — real environment/evidence required |
| IRS-099 | Privacy/data classification | Gateway/KMS secret isolation, redaction and classification/purpose-based minimization. | Adversarially mutate/bypass privacy/data classification and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — implementation + execution required |
| IRS-100 | Independent release review | Pinned build graph, signed SBOM/provenance/artifact and independent release verification. | Adversarially mutate/bypass independent release review and assert expected DENY/STOP plus zero unauthorized side effect. | Executable result + audit evidence + frozen artifact/config digest + reviewer | UNPROVEN — real environment/evidence required |

## Required evidence record

```yaml
subject_id: IRS-XXX
attack: "..."
precondition: "..."
expected: "..."
actual: "..."
side_effect: "..."
audit_evidence: "..."
verdict: PASS|FAIL|BLOCKED|UNPROVEN
root_cause: "..."
fix: "..."
regression: "..."
reviewer: "..."
review_timestamp: "..."
```

## Release gates

- 0 P0 authorization/tenant/replay/gateway/credential/audit/kill-switch failures.
- No unexplained critical UNPROVEN/BLOCKED subjects.
- Clean `npm ci`, full typecheck, full tests, production build and dependency audit.
- PostgreSQL multi-instance replay/budget/failover evidence.
- KMS/HSM signing and key-rotation evidence.
- Real egress proxy + DNS-rebinding evidence.
- Backup/restore anti-resurrection evidence.
- Signed SBOM/provenance/artifact verification.
- Independent reviewer signs exact frozen release packet.

## Current hard blocker

The V4 source dynamically loads `@aws-sdk/client-s3` for S3 mode, but the runtime dependency is absent from package.json/package-lock.json. The correct fix is to add the dependency and regenerate the lockfile in a clean network-enabled build environment; an invented lock entry would be unsafe.
