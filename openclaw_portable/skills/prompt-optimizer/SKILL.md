---
name: prompt-optimizer
description: Use when asked to optimize a prompt for a specific LLM (Claude, GPT, Gemini, Llama), benchmark prompt variants, or auto-tune prompts for cost, accuracy or latency. Trigger phrases: 'optimize this prompt for', 'which prompt works best', 'tune my prompt', 'model-specific prompt'. Produces model-profiled optimized prompt variants with rationale.
---

# Prompt / Model Optimizer

(Original SKILL.md missing; see bundled files.)
